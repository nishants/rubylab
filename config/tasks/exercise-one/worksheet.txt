# Return first name
def first_name(page)
  page[:name]
end

# Return full name
def full_name(page)
  nil
end

# Return last name
def last_name(page)
  ""
end

# Return year of birth as YYYY
def year_of_birth(page)
  1965
end

# Return name of current city
def current_city(page)
  "Bangalore"
end


# Return name of past cities as array
def past_cities(page)
  ["Mumbai", ["New York"]]
end

# Return dates of address change as MM/YY, from oldest to newest
def address_change_dates(page)
  []
end
